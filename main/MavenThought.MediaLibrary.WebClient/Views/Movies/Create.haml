%h2 Add movies to the library

- using( Html.BeginForm("Create", "Movies") )
  = Html.Label("Movie Title")
  = Html.TextBox("title", string.Empty, new { id = "movie_title" })
  = Html.Submit("Submit")
